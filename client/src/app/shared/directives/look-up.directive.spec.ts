import { LookUpDirective } from './look-up.directive';

describe('LookUpDirective', () => {
  it('should create an instance', () => {
    const directive = new LookUpDirective();
    expect(directive).toBeTruthy();
  });
});
